#include <iostream>

int main(void)
{
    /* 3.4.1.1 Declaring and defining functions
     *  if you omit the return_type, the compiler assumes that the function returns a value of type int
    */

    /*
     * 3.5.1.5 Transfering data to and from functions
     *  If a parameter is declared as passed by reference (so it is preceded by the & sign) its corresponding actual 
     *  parameter must be a variable.
     *  An actual parameter referring to a “passed by value” formal parameter may be an expression in general, so we can 
     *  use not only a variable but also a literal, or even a function invocation's result.
    */

    /*
     * 3.6.1.3 Default parameters
     *  if more than one parameter is declared with a default value and at least one actual parameter is specified in the 
     *  invocation, the actual parameters are assigned to their formal counterparts in the same order in which they are listed 
     *  in the function declaration; this means that you are not allowed to use the default value for the first parameter and 
     *  specify an explicit value for the second
    */

    /*
     * 3.6.1.1 Default parameters
     *
    */



    /* 3.8.1.5 Overloaded functions
     *  1.0f literal for float, 1.0 literal for double, no promotion from more to less precise
    */
}